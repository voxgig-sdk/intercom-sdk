# Intercom SDK

Intercom API client, generated from the OpenAPI spec.

The intercom API reference.

Learn more about Intercom API at [intercom.io](https://intercom.io).

This is an unofficial SDK for the Intercom public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Full documentation for this SDK: [https://voxgig-sdk.github.io/intercom-sdk/](https://voxgig-sdk.github.io/intercom-sdk/)

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `debug`, `idempotency`, `metrics`, `paging`, `ratelimit`, `retry`, `test`, `timeout` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as **89 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new IntercomSDK()
const items = await client.ActivityLog().list({ created_at_after: "example" })
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = IntercomSDK.test({
  entity: {
    contact_segment: {
      test01: { id: 'test01' },
    },
  },
})
const contactsegments = await client.ContactSegment().list()
// contactsegments is an array of ContactSegment entities, populated with mock data
// — call contactsegments[0].data() for the record itself
console.log(contactsegments)
```

### Python

```python
client = IntercomSDK.test()
contactsegments = client.ContactSegment().list()
print(contactsegments)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = IntercomSDK::test([
    "entity" => ["contactsegment" => ["test01" => []]],
]);
$contactsegments = $client->ContactSegment()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.ContactSegment(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:ContactSegment():list()
```

### JavaScript

```js
const client = IntercomSDK.test()
const contactsegments = await client.ContactSegment().list()
// contactsegments is an array of entities, populated with mock data
// — call contactsegments[0].data() for the record itself
console.log(contactsegments)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/intercom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/intercom-sdk/tags) |
| Python | `voxgig-sdk-intercom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/intercom-sdk/tags) |
| PHP | `voxgig-sdk/intercom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/intercom-sdk/tags) |
| Golang | `github.com/voxgig-sdk/intercom-sdk/go` | `go get github.com/voxgig-sdk/intercom-sdk/go@latest` |
| Lua | `voxgig-sdk-intercom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/intercom-sdk/tags) |
| JavaScript | `@voxgig-sdk/intercom-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/intercom-sdk/tags) |
| Go CLI | `github.com/voxgig-sdk/intercom-sdk/go-cli` | `go install github.com/voxgig-sdk/intercom-sdk/go-cli/cmd/intercom@latest` |
| Go MCP server | `github.com/voxgig-sdk/intercom-sdk/go-mcp` | `go get github.com/voxgig-sdk/intercom-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { IntercomSDK } from '@voxgig-sdk/intercom'

const client = new IntercomSDK({
  apikey: process.env.INTERCOM_APIKEY,
})

// List all activitylogs (returns ActivityLogEntity[] — .data() for the record)
const activitylogs = await client.ActivityLog().list({ created_at_after: "example" })
for (const activitylog of activitylogs) {
  console.log(activitylog)
}

// Load a specific aicall (returns a AiCall)
const aicall = await client.AiCall().load({
  conversation_id: 'example_conversation_id',
})
console.log(aicall)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o intercom-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "intercom": {
      "command": "/abs/path/to/intercom-mcp"
    }
  }
}
```

## Entities

The API exposes 89 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **ActivityLog** | The ActivityLog entity (list). | `/admins/activity_logs` |
| **ActivityLogEventType** | The ActivityLogEventType entity (list). | `/admins/activity_log_event_types` |
| **ActivityLogList** | The ActivityLogList entity (create). | `/admins/activity_logs/search` |
| **Admin** | The Admin entity (list, load, update). | `/admins` |
| **AdminWithApp** | The AdminWithApp entity (list). | `/me` |
| **AiCall** | The AiCall entity (create, load). | `/fin_voice/conversation/{conversation_id}` |
| **AiContent** | The AiContent entity (remove). | `/ai/content_import_sources/{source_id}` |
| **Article** | The Article entity (create, list, load, update). | `/articles` |
| **ArticleSearch** | The ArticleSearch entity (load). | `/articles/search` |
| **ArticleVersion** | The ArticleVersion entity (load). | `/articles/{article_id}/versions/{id}` |
| **ArticleVersionList** | The ArticleVersionList entity (list). | `/articles/{article_id}/versions` |
| **Audience** | The Audience entity (create, list, load, remove, update). | `/audiences` |
| **AwayStatusReason** | The AwayStatusReason entity (list). | `/away_status_reasons` |
| **Banner** | The Banner entity (list). | `/contacts/{id}/banners` |
| **BannerDismiss** | The BannerDismiss entity (create). | `/contacts/{id}/banners/{view_id}/dismiss` |
| **Brand** | The Brand entity (list, load). | `/brands` |
| **Call** | The Call entity (create, list, load). | `/calls` |
| **Company** | The Company entity (create, list, load, remove, update). | `/companies` |
| **CompanyAttachedContact** | The CompanyAttachedContact entity (list). | `/companies/{company_id}/contacts` |
| **CompanyAttachedSegment** | The CompanyAttachedSegment entity (list). | `/companies/{company_id}/segments` |
| **CompanyList** | The CompanyList entity (create). | `/companies/list` |
| **CompanyScroll** | The CompanyScroll entity (list). | `/companies/scroll` |
| **Contact** | The Contact entity (create, list, load, remove, update). | `/contacts` |
| **ContactAttachedCompany** | The ContactAttachedCompany entity (list). | `/contacts/{contact_id}/companies` |
| **ContactList** | The ContactList entity (create). | `/contacts/search` |
| **ContactSegment** | The ContactSegment entity (list). | `/contacts/{contact_id}/segments` |
| **Content** | The Content entity (create). | `/content/bulk_actions` |
| **ContentImportSource** | The ContentImportSource entity (create, list, load, update). | `/ai/content_import_sources` |
| **ContentSearch** | The ContentSearch entity (list). | `/content/search` |
| **ContentSnippet** | The ContentSnippet entity (create, list, load, remove, update). | `/content_snippets` |
| **Conversation** | The Conversation entity (create, list, load, remove, update). | `/conversations` |
| **ConversationAttribute** | The ConversationAttribute entity (create, load, remove, update). | `/conversations/attributes/{id}` |
| **ConversationAttributeList** | The ConversationAttributeList entity (list). | `/conversations/attributes` |
| **ConversationList** | The ConversationList entity (create). | `/conversations/search` |
| **ConversationParticipant** | The ConversationParticipant entity (create, remove). | `/conversations/{conversation_id}/customers` |
| **CustomObjectInstance** | The CustomObjectInstance entity (create, load, remove). | `/custom_object_instances/{custom_object_type_identifier}` |
| **Data** | The Data entity (create, load). | `/download/content/data/{job_identifier}` |
| **DataAttribute** | The DataAttribute entity (create, list, update). | `/data_attributes` |
| **DataConnector** | The DataConnector entity (create, list, load, update). | `/data_connectors` |
| **DataConnectorExecutionResult** | The DataConnectorExecutionResult entity (load). | `/data_connectors/{data_connector_id}/execution_results/{id}` |
| **DataConnectorExecutionResultList** | The DataConnectorExecutionResultList entity (list). | `/data_connectors/{data_connector_id}/execution_results` |
| **DataEvent** | The DataEvent entity (create). | `/events` |
| **DataEventSummary** | The DataEventSummary entity (list). | `/events` |
| **DataExport** | The DataExport entity (create). | `/export/cancel/{job_identifier}` |
| **Deleted** | The Deleted entity (list). | `/conversations/deleted` |
| **DeletedArticleObject** | The DeletedArticleObject entity (remove). | `/articles/{article_id}` |
| **DeletedCompanyObject** | The DeletedCompanyObject entity (remove). | `/companies/{company_id}` |
| **DeletedDataConnectorObject** | The DeletedDataConnectorObject entity (remove). | `/data_connectors/{id}` |
| **DeletedInternalArticleObject** | The DeletedInternalArticleObject entity (create, list, remove). | `/internal_articles` |
| **DeletedObject** | The DeletedObject entity (remove). | `/news/news_items/{news_item_id}` |
| **Email** | The Email entity (list, load). | `/emails` |
| **ExternalPage** | The ExternalPage entity (create, list, load, remove, update). | `/ai/external_pages` |
| **FinAgent** | The FinAgent entity (create). | `/fin/csat` |
| **HandlingEvent** | The HandlingEvent entity (list). | `/conversations/{id}/handling_events` |
| **HelpCenter** | The HelpCenter entity (create, list, load, remove, update). | `/help_center/help_centers` |
| **InternalArticle** | The InternalArticle entity (load, update). | `/internal_articles/{internal_article_id}` |
| **InternalArticleSearch** | The InternalArticleSearch entity (load). | `/internal_articles/search` |
| **IpAllowlist** | The IpAllowlist entity (list, update). | `/ip_allowlist` |
| **Job** | The Job entity (create, load). | `/jobs/status/{job_id}` |
| **Macro** | The Macro entity (list, load). | `/macros` |
| **MergeHistory** | The MergeHistory entity (list). | `/contacts/{id}/merge_history` |
| **Message** | The Message entity (create). | `/messages` |
| **NewsItem** | The NewsItem entity (create, load, update). | `/news/news_items/{news_item_id}` |
| **Newsfeed** | The Newsfeed entity (load). | `/news/newsfeeds/{newsfeed_id}` |
| **Note** | The Note entity (create, list, load). | `/companies/{company_id}/notes` |
| **OfficeHour** | The OfficeHour entity (create, list, remove). | `/office_hours_schedules` |
| **OfficeHoursException** | The OfficeHoursException entity (create, list, load, update). | `/office_hours_schedules/{office_hours_schedule_id}/office_hours_exceptions` |
| **OfficeHoursSchedule** | The OfficeHoursSchedule entity (load, update). | `/office_hours_schedules/{id}` |
| **Paginated** | The Paginated entity (list). | `/news/newsfeeds/{newsfeed_id}/items` |
| **PhoneSwitch** | The PhoneSwitch entity (create). | `/phone_call_redirects` |
| **ReportingData** | The ReportingData entity (load). | `/download/reporting_data/{job_identifier}` |
| **ReportingDataExport** | The ReportingDataExport entity (create, list). | `/export/reporting_data/get_datasets` |
| **Segment** | The Segment entity (list, load). | `/segments` |
| **SideConversation** | The SideConversation entity (list). | `/conversations/{id}/side_conversations` |
| **Subscription** | The Subscription entity (create, list, remove). | `/contacts/{contact_id}/subscriptions` |
| **SubscriptionType** | The SubscriptionType entity (list). | `/subscription_types` |
| **Tag** | The Tag entity (create, list, load, remove). | `/contacts/{contact_id}/tags` |
| **Team** | The Team entity (list, load). | `/teams` |
| **TeamMetricList** | The TeamMetricList entity (list). | `/teams/{team_id}/metrics` |
| **Ticket** | The Ticket entity (create, load, remove, update). | `/tickets/{ticket_id}` |
| **TicketList** | The TicketList entity (create). | `/tickets/search` |
| **TicketReply** | The TicketReply entity (create). | `/tickets/{ticket_id}/reply` |
| **TicketState** | The TicketState entity (list). | `/ticket_states` |
| **TicketType** | The TicketType entity (create, list, load, update). | `/ticket_types` |
| **TicketTypeAttribute** | The TicketTypeAttribute entity (create, update). | `/ticket_types/{ticket_type_id}/attributes` |
| **Visitor** | The Visitor entity (load, update). | `/visitors` |
| **WhatsappMessageStatus** | The WhatsappMessageStatus entity (load). | `/messages/whatsapp/status` |
| **WhatsappMessageStatusList** | The WhatsappMessageStatusList entity (list). | `/messages/status` |
| **Workflow** | The Workflow entity (load). | `/export/workflows/{id}` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from intercom_sdk import IntercomSDK

client = IntercomSDK({
    "apikey": os.environ.get("INTERCOM_APIKEY"),
})

# List all activitylogs (returns a list, raises on error)
activitylogs = client.ActivityLog().list({"created_at_after": "example"})
for activitylog in activitylogs:
    print(activitylog)
```

### PHP

```php
<?php
require_once 'intercom_sdk.php';

$client = new IntercomSDK([
    "apikey" => getenv("INTERCOM_APIKEY"),
]);

// List all activitylogs (returns an array; throws on error)
$activitylogs = $client->ActivityLog()->list();
print_r(array_map(fn($item) => $item->data_get(), $activitylogs));
```

### Golang

```go
import sdk "github.com/voxgig-sdk/intercom-sdk/go"

client := sdk.NewIntercomSDK(map[string]any{
    "apikey": os.Getenv("INTERCOM_APIKEY"),
})

// List all activitylogs
activityLogs, err := client.ActivityLog(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(activityLogs)

// Load a specific aicall
aiCall, err := client.AiCall(nil).Load(
    map[string]any{"conversation_id": "example_conversation_id"}, nil,
)
if err != nil {
    panic(err)
}
fmt.Println(aiCall)
```

### Lua

```lua
local sdk = require("intercom_sdk")

local client = sdk.new({
  apikey = os.getenv("INTERCOM_APIKEY"),
})

-- List all activitylogs
local activitylogs, err = client:ActivityLog():list()
print(activitylogs)
```

### JavaScript

```js
const { IntercomSDK } = require('@voxgig-sdk/intercom-js')

const client = new IntercomSDK({
  apikey: process.env.INTERCOM_APIKEY,
})

// List all activitylogs (returns an array)
const activitylogs = await client.ActivityLog().list({ created_at_after: "example" })
for (const activitylog of activitylogs) {
  console.log(activitylog)
}

// Load a specific aicall (returns the entity)
const aicall = await client.AiCall().load({
  conversation_id: 'example_conversation_id',
})
console.log(aicall)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **DebugFeature** | Request/response capture ring buffer for debugging |
| **IdempotencyFeature** | Idempotency keys for safe retries of mutating operations |
| **MetricsFeature** | Statistics capture: per-operation counters and latency |
| **PagingFeature** | Pagination signals for list operations |
| **RatelimitFeature** | Client-side rate limiting via a token bucket |
| **RetryFeature** | Automatic retry of transient failures with exponential backoff |
| **TestFeature** | In-memory mock transport for testing without a live server |
| **TimeoutFeature** | Per-request timeout with transport abort |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://aontu.dev), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://developers.intercom.com](https://developers.intercom.com)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Intercom API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
