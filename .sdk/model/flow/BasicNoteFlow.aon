# BasicNoteFlow

main: kit: flow: BasicNoteFlow:
{
  entity: "note",
  kind: "basic",
  name: "BasicNoteFlow",
  step: [
    {
      input: {
        ref: "note_ref01"
      }

      match: {
        company_id: "company01",
        contact_id: "contact01"
      }

      op: "create"
    }

    {
      match: {
        contact_id: "contact01"
      }

      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "note_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "note_ref01",
        srcdatavar: "note_ref01_data",
        suffix: "_dt0"
      }

      match: {
        id: "note01"
      }

      op: "load",
      valid: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-note_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicNoteFlow"
}